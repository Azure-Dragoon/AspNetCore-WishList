@model List<WishList.Models.Item>
<h1>Wishlist</h1>
<a asp-action="create">Add Item</a>
 @foreach (var item in Model)
            {
                <ul>item</ul>
                <li>item.Description<a asp-action="delete" asp-route="@item.Id">delete</a></li>
            }
